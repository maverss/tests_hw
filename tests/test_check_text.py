from pages.demoqa import DemoQa
from pages.elements_page import ElementPage

'''
def test_go_to_page_elements(browser):
    demo_qa_page = DemoQa(browser)
    elements_page = ElementPage(browser)
    
    demo_qa_page.visit()
    assert demo_qa_page.get_text('span') == '© 2013-2020 TOOLSQA.COM | ALL RIGHTS RESERVED.'

    elements_page.visit()
    assert elements_page.get_text('#app > div > div > div > div:nth-child(2)') == 'Please select an item from left to start practice.'

def test_page_elements(browser):
    el_page = ElementPage(browser)

    el_page.visit()
    assert el_page.get_text('div.col-12:nth-child(2)') == 'Please select an item from left to start practice.'
    
    assert el_page.icon.exist('div:nth-child(1) > span > div > ul > #item-0 > span')
    assert el_page.btn_sidebar_first.exist('div:nth-child(1) > span > div')
    assert el_page.btn_sidebar_first_textbox.exist('header > a > img')

def test_page_elements_2(browser):
    el_page = ElementPage(browser)
    el_page.visit()
    
    
    assert el_page.icon.exist() == 'Element found'
    assert el_page.btn_sidebar_first.exist() == 'Element found'
    assert el_page.btn_sidebar_first_textbox.exist() == 'Element found'
'''


